<%= form_with(model: [@project,@todo]) do |form| %>
  <% if @todo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@todo.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% @todo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>


    </div>
  <% end %>

  <%= form.text_area :description,  placeholder: "Enter the Todo"%>
  <br>

  <%= form.label 'Mark as done' %>
  <%= form.check_box :status %>
  <br>
  <br>
  <div>
    <%= form.submit %>
  </div>
<% end %>
